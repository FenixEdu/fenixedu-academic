<?xml version="1.0" encoding="ISO-8859-1"?>
<project basedir="." default="compile-proj" name="CIAPL">

	<property name="sshtools.home" location="lib"/>

    <property environment="env"/>

	<property file="build.properties"/>

    <property name="src" location="src"/>
	<property name="build.home" location="build"/>

	<property name="webapp"          location="${build.home}/app"/>
    <property name="webapp.metadata" value="${webapp}/WEB-INF"/>
    <property name="webapp.classes"  value="${webapp.metadata}/classes"/>
    <property name="webapp.lib"      value="${webapp.metadata}/lib"/>

    <property name="lib" location="lib"/>
	<property location="lib_web" name="libs.web"/>

    <property location="config" name="config"/>

    <property location="etc" name="sql"/>

	<property name="compile.debug"       value="on"/>
  	<property name="compile.deprecation" value="on"/>
  	<property name="compile.optimize"    value="on"/>

	<property name="encoding" value="ISO-8859-15"/>	

    <target name="clean-all" description="Removes any generated files">
		<delete dir="${build.home}"/>
    </target>

	<target name="prepare">
		<mkdir dir="${webapp.classes}"/>	
		<mkdir dir="${webapp.lib}"/>

		<copy todir="${webapp.metadata}">
			<fileset dir="${basedir}/metadata"/>    
		</copy>

		<copy todir="${webapp.lib}">
			<fileset dir="${basedir}/lib">    
				<exclude name="servlet.jar"/>
				<exclude name="strutstest.jar"/>
			</fileset>
			<fileset dir="${basedir}/lib_web">
				<exclude name="servlet.jar"/>
				<exclude name="strutstest.jar"/>
			</fileset>
		</copy>

		<copy todir="${webapp.classes}">
			<fileset dir="${basedir}/config"/>
		</copy>
	</target>
  
    <target name="compile-proj"
            description="Compila o projecto principal" depends="prepare">
        <tstamp/>
        <javac destdir="${webapp.classes}"
               extdirs="${lib};${libs.web}"
               debug="${compile.debug}"
               optimize="${compile.optimize}"
               deprecation="${compile.deprecation}"
               encoding="${encoding}"
               memoryInitialSize="512m"
               memoryMaximumSize="512m"
               fork="yes">
			<src path="${src}"/>
        </javac>
		<replace dir="${webapp.metadata}" propertyFile="build.properties">
	    	<include name="**/**/*.xml"/>
	    	<include name="**/**/ApplicationResources.properties"/>
	    	<include name="**/ServidorPersistenteConfig.properties"/>
	    	<include name="**/ServidorPersistenteOracleConfig.properties"/>

			<replacefilter token="@db.user@" property="db.user"/>
			<replacefilter token="@db.pass@" property="db.pass"/>
			<replacefilter token="@db.name@" property="db.name"/>
			<replacefilter token="@db.alias@" property="db.alias"/>
		</replace>
    </target>

    <target name="create-tables" description="">
        <sql
            driver="com.mysql.jdbc.Driver"
            url="jdbc:mysql:${db.alias}"
            userid="${db.user}"
            password="${db.pass}"
            encoding="${encoding}">
	        <fileset dir="${sql}">
				<include name="middleware/commons/createTables.sql"/>
  			</fileset>
            <classpath>
                <fileset dir="${lib}">
                    <include name="**/*.jar"/>
                </fileset>
            </classpath>
        </sql>
    </target>

	<target name="replace-tokens">
        <delete file="${sql}/middleware/commons/PopulateTables_generated.sql"/>
		<copy file="${sql}/middleware/commons/PopulateTables.sql" toFile="${sql}/middleware/commons/PopulateTables_generated.sql"/>
		<replace dir="${sql}/middleware/commons" propertyFile="middleware.properties">
	    	<include name="PopulateTables_generated.sql"/>
		    <replacefilter token="@load.data.infile.root@" property="load.data.infile.root"/>
		    <replacefilter token="@load.data.infile.rootIleec@" property="load.data.infile.rootIleec"/>
    	</replace>
	</target>

    <target name="load-data-from-old-system" depends="create-tables, replace-tokens" description="">
        <sql
            driver="com.mysql.jdbc.Driver"
            url="jdbc:mysql:${db.alias}"
            userid="${db.user}"
            password="${db.pass}"
            encoding="${encoding}">
	        <fileset dir="${sql}">
				<include name="middleware/commons/PopulateTables_generated.sql"/>
  			</fileset>
            <classpath>
                <fileset dir="${lib}">
                    <include name="**/*.jar"/>
                </fileset>
            </classpath>
        </sql>
    </target>

	<!-- ********************** EQUIVALENCE MIGRATION RELATED ********************** -->
	<target name="make-equivalences-for-all-students-past-enrolments" depends="compile-proj">
		<java classname="middleware.studentMigration.equivalences.MakeEquivalencesForAllStudentsPastEnrolments" fork="yes" failonerror="false" maxmemory="768m">
			<classpath>
				<pathelement path="${webapp.classes}"/>
				<pathelement path="${config}"/>
				<pathelement path="${sql}"/>
				<pathelement path="${src}"/>
				<fileset dir="${webapp.lib}">
					<include name="**/*.jar"/>
				</fileset>
			</classpath>
		</java>
	</target>
	<!-- *************************************************************************** -->

	<!-- ********************** ILEEC MIGRATION RELATED ********************** -->
	<target name="replace-tokens-for-ileec">
		<delete file="${sql}/middleware/commons/PopulateTables_generated.sql"/>
		<copy file="${sql}/middleware/ileec/populateTablesIleec.sql" toFile="${sql}/middleware/commons/PopulateTables_generated.sql"/>
		<replace dir="${sql}/middleware/commons" propertyFile="middleware.properties">
			<include name="PopulateTables_generated.sql"/>
			<replacefilter token="@load.data.infile.rootIleec@" property="load.data.infile.rootIleec"/>
		</replace>
	</target>

	<target name="create-tables-ileec" depends="" description="Cria as tabelas de middleware para suportar os dados do ILEEC">
		<sql driver="com.mysql.jdbc.Driver" url="jdbc:mysql://localhost/${db.name}" userid="${db.user}" password="${db.pass}" encoding="${encoding}">
			<fileset dir="${sql}">
				<include name="middleware/ileec/createTablesIlecc.sql"/>
			</fileset>
			<classpath>
				<fileset dir="${lib}">
					<include name="**/*.jar"/>
				</fileset>
			</classpath>
		</sql>
	</target>

	<target name="load-ileec-data" depends="create-tables-ileec, replace-tokens-for-ileec" description="Carrega os dados do ILEEC nas tabelas de middleware">
		<sql driver="com.mysql.jdbc.Driver" url="jdbc:mysql://localhost/${db.name}" userid="${db.user}" password="${db.pass}" encoding="${encoding}">
			<fileset dir="${sql}">
				<include name="middleware/commons/PopulateTables_generated.sql"/>
			</fileset>
			<classpath>
				<fileset dir="${lib}">
					<include name="**/*.jar"/>
				</fileset>
			</classpath>
		</sql>
	</target>

	<target name="make-equivalences-for-ileec-students" depends="compile-proj, load-ileec-data" description="Cria na BD do Fénix as equivalências do ILEEC">
		<java classname="middleware.studentMigration.equivalences.MakeEquivalencesForILEECStudents" fork="yes" failonerror="false" maxmemory="768m">
			<classpath>
				<pathelement path="${webapp.classes}"/>
				<pathelement path="${config}"/>
				<pathelement path="${sql}"/>
				<pathelement path="${src}"/>
				<fileset dir="${webapp.lib}">
					<include name="**/*.jar"/>
				</fileset>
			</classpath>
		</java>
	</target>

	<!-- ********************************************************************* -->
	
	<target name="update-LEEC-degree-curricular-plan-for-enrollement" depends="load-ileec-data" description="">
		<sql driver="com.mysql.jdbc.Driver" url="jdbc:mysql://localhost/${db.name}" userid="${db.user}" password="${db.pass}" encoding="${encoding}">
			<fileset dir="${sql}">
				<include name="middleware/ileec/updateLEECDegreeCurricularPlanForEnrollement.sql"/>
			</fileset>
			<classpath>
				<fileset dir="${lib}">
					<include name="**/*.jar"/>
				</fileset>
			</classpath>
		</sql>
	</target>

	<target name="migrate-ileec-scientific-areas" depends="compile-proj, load-ileec-data">
		<java classname="middleware.studentMigration.ileecDataMigration.migrateILeecScientificAreas" fork="yes" failonerror="false" maxmemory="512m">
			<classpath>
				<pathelement path="${webapp.classes}"/>
				<pathelement path="${config}"/>
				<pathelement path="${sql}"/>
				<pathelement path="${src}"/>
				<fileset dir="${webapp.lib}">
					<include name="**/*.jar"/>
				</fileset>
			</classpath>
		</java>
	</target>	

	<target name="associate-ileecCourses-toScientificAreas" depends="compile-proj, load-ileec-data">
		<java classname="middleware.studentMigration.ileecDataMigration.associateILeecCoursesToScientificAreas" fork="yes" failonerror="false" maxmemory="512m">
			<classpath>
				<pathelement path="${webapp.classes}"/>
				<pathelement path="${config}"/>
				<pathelement path="${sql}"/>
				<pathelement path="${src}"/>
				<fileset dir="${webapp.lib}">
					<include name="**/*.jar"/>
				</fileset>
			</classpath>
		</java>
	</target>
	
	<target name="migrate-IleecGroups-Data" depends="compile-proj, load-ileec-data">
		<java classname="middleware.studentMigration.ileecDataMigration.migrateILeecGroupsData" fork="yes" failonerror="false" maxmemory="512m">
			<classpath>
				<pathelement path="${webapp.classes}"/>
				<pathelement path="${config}"/>
				<pathelement path="${sql}"/>
				<pathelement path="${src}"/>
				<fileset dir="${webapp.lib}">
					<include name="**/*.jar"/>
				</fileset>
			</classpath>
		</java>
	</target>

	<target name="migrate-precedence-by-number-curricular-courses" depends="compile-proj, load-ileec-data">
		<java classname="middleware.studentMigration.ileecDataMigration.migrateILeecPrecedenceDataByNumberOfDoneCurricularCourses" fork="yes" failonerror="false" maxmemory="512m">
			<classpath>
				<pathelement path="${webapp.classes}"/>
				<pathelement path="${config}"/>
				<pathelement path="${sql}"/>
				<pathelement path="${src}"/>
				<fileset dir="${webapp.lib}">
					<include name="**/*.jar"/>
				</fileset>
			</classpath>
		</java>
	</target>

	<target name="migrate-precedence-by-curricular-courses" depends="compile-proj, load-ileec-data">
		<java classname="middleware.studentMigration.ileecDataMigration.migrateILeecPrecedenceDataByCurricularCourse" fork="yes" failonerror="false" maxmemory="512m">
			<classpath>
				<pathelement path="${webapp.classes}"/>
				<pathelement path="${config}"/>
				<pathelement path="${sql}"/>
				<pathelement path="${src}"/>
				<fileset dir="${webapp.lib}">
					<include name="**/*.jar"/>
				</fileset>
			</classpath>
		</java>
	</target>
	
	<target name="migrate-all-ileec-data" depends="compile-proj, load-ileec-data, migrate-ileec-scientific-areas, associate-ileecCourses-toScientificAreas, migrate-IleecGroups-Data, migrate-precedence-by-number-curricular-courses, migrate-precedence-by-curricular-courses">
	</target>

	<!-- ********************************************************************* -->
	
	<target name="verify-LEEC-curricular-plan" depends="compile-proj">
		<java classname="UtilInsc.VerifyLEECCurricularPlan" fork="yes" failonerror="false" maxmemory="512m">
			<classpath>
				<pathelement path="${webapp.classes}"/>
				<pathelement path="${config}"/>
				<pathelement path="${sql}"/>
				<pathelement path="${src}"/>
				<fileset dir="${webapp.lib}">
					<include name="**/*.jar"/>
				</fileset>
			</classpath>
		</java>
	</target>

</project>