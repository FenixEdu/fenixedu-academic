<!--
Define here all used connections.
One defined connection should be defined as the default one,
by set default-connection="true" - this could be done at runtime too.

It is possible to set user/password at
runtime or let login different users at runtime using the same
database. Use different PBKey with same jcdAlias name but
different user/password.

Ditto it is possible to add jdbc-connection-descriptor at runtime
using the MetadataManager.
-->


    <!-- this connection was used as the default one within OJB -->
    <jdbc-connection-descriptor
   		jcd-alias="OJB/repository.xml"
   		default-connection="true"
   		platform="MySQL"
   		jdbc-level="1.0"
   		driver="com.mysql.jdbc.Driver"
   		protocol="jdbc"
   		subprotocol="mysql"
   		dbalias="@db.alias@"
   		username="@db.user@"
   		password="@db.pass@"
   		batch-mode="false"
        useAutoCommit="2"
        ignoreAutoCommitExceptions="false"
     >

        <connection-pool
            maxActive="21"
            maxIdle="5"
            validationQuery="select 1"
            whenExhaustedAction="2"/>

        <sequence-manager className="org.apache.ojb.broker.util.sequence.SequenceManagerHighLowImpl">
            <attribute attribute-name="grabSize" attribute-value="20"/>
        </sequence-manager>
   </jdbc-connection-descriptor>

    <jdbc-connection-descriptor
   		jcd-alias="assiduousness"
   		default-connection="false"
   		platform="@db.assiduousness.platform@"
   		jdbc-level="2.0"
   		driver="@db.assiduousness.driver@"
   		protocol="@db.assiduousness.protocol@"
   		subprotocol="@db.assiduousness.subprotocol@"
   		dbalias="@db.assiduousness.alias@"
   		username="@db.assiduousness.user@"
   		password="@db.assiduousness.pass@"
   		batch-mode="false"
        useAutoCommit="2"
        ignoreAutoCommitExceptions="false"
     >

        <connection-pool
            maxActive="21"
            validationQuery="@db.assiduousness.validationQuery@"
            whenExhaustedAction="2"/>

<!--        <sequence-manager className="org.apache.ojb.broker.util.sequence.SequenceManagerHighLowImpl">
            <attribute attribute-name="grabSize" attribute-value="20"/>
        </sequence-manager> -->
   </jdbc-connection-descriptor>


    <jdbc-connection-descriptor
   		jcd-alias="personFilter"
   		default-connection="false"
   		platform="@db.personFilter.platform@"
   		jdbc-level="2.0"
   		driver="@db.personFilter.driver@"
   		protocol="@db.personFilter.protocol@"
   		subprotocol="@db.personFilter.subprotocol@"
   		dbalias="@db.personFilter.alias@"
   		username="@db.personFilter.user@"
   		password="@db.personFilter.pass@"
   		batch-mode="false"
        useAutoCommit="2"
        ignoreAutoCommitExceptions="false"
     >

        <connection-pool
            maxActive="21"
            validationQuery="@db.personFilter.validationQuery@"
            whenExhaustedAction="2"/>

<!--        <sequence-manager className="org.apache.ojb.broker.util.sequence.SequenceManagerHighLowImpl">
            <attribute attribute-name="grabSize" attribute-value="20"/>
        </sequence-manager> -->
   </jdbc-connection-descriptor>
