CREATE TABLE `ACADEMIC_SERVICE_REQUEST_YEAR` (
	`ID_INTERNAL` INTEGER NOT NULL AUTO_INCREMENT,
	`KEY_ROOT_DOMAIN_OBJECT` INTEGER NOT NULL DEFAULT '1',

	`YEAR` INTEGER,
	`LATEST_SERVICE_REQUEST_NUMBER` INTEGER,
 
	PRIMARY KEY (ID_INTERNAL),
	UNIQUE KEY `YEAR` (`YEAR`)
) TYPE=INNODB;

ALTER TABLE `ACADEMIC_SERVICE_REQUEST` ADD COLUMN KEY_ACADEMIC_SERVICE_REQUEST_YEAR INT(11) DEFAULT NULL;
