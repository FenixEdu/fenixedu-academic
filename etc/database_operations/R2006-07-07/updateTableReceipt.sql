select concat('update RECEIPT set RECEIPT.WHEN_CREATED="', RECEIPT_PRINT_VERSION.WHEN_CREATED, '" where RECEIPT.ID_INTERNAL=', RECEIPT_PRINT_VERSION.KEY_RECEIPT, ';') as "" from RECEIPT_PRINT_VERSION;
