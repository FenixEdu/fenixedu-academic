
ALTER TABLE PHD_ALERT ADD COLUMN WHEN_CREATED DATETIME NULL DEFAULT NULL;

UPDATE PHD_ALERT SET PHD_ALERT.WHEN_CREATED = NOW();

ALTER TABLE PHD_ALERT CHANGE COLUMN WHEN_CREATED WHEN_CREATED DATETIME NOT NULL;
