select concat('update STUDENT set STUDENT.KEY_DELEGATE = ', DELEGATE.ID_INTERNAL, ' where STUDENT.ID_INTERNAL = ', STUDENT.ID_INTERNAL, ';') as "" from DELEGATE inner join STUDENT on STUDENT.ID_INTERNAL = DELEGATE.KEY_STUDENT;