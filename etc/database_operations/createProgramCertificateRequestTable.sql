CREATE TABLE PROGRAM_CERTIFICATE_REQUEST_ENROLMENT (
  KEY_PROGRAM_CERTIFICATE_REQUEST INT(11) NOT NULL,
  KEY_ENROLMENT INT(11) NOT NULL,
  PRIMARY KEY (KEY_PROGRAM_CERTIFICATE_REQUEST, KEY_ENROLMENT),
  KEY (KEY_PROGRAM_CERTIFICATE_REQUEST),
  KEY (KEY_ENROLMENT)
) ENGINE=InnoDB;
