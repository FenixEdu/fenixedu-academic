CREATE TABLE FF$PERSISTENT_ROOT (
	OID BIGINT NOT NULL,
	ROOT BIGINT default NULL,
	PRIMARY KEY  (OID)
) TYPE=InnoDB;

INSERT INTO FF$PERSISTENT_ROOT VALUES (1, null);
UPDATE FF$PERSISTENT_ROOT SET ROOT=(SELECT OID FROM ROOT_DOMAIN_OBJECT WHERE ID_INTERNAL=1) WHERE OID=1;
